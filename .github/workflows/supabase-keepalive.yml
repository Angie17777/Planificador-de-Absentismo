name: Supabase Keepalive

"on":
  workflow_dispatch:
  schedule:
    - cron: "0 */12 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run:  "curl -I https://uuligswbxflpnhaqjtuh.supabase.co"
